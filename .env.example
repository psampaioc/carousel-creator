# Browser-safe identifiers
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
NEXT_PUBLIC_CONVEX_URL=

# Server only (configure in both Vercel and Convex where used)
CLERK_SECRET_KEY=
OPERATOR_EMAIL=

# Convex deployment environment only
CLERK_JWT_ISSUER_DOMAIN=
GOOGLE_SHEET_ID=
GOOGLE_DRIVE_PROJECT_FOLDER_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=

# Use the `Candidates` tab and the fixed headers documented in docs/operations.
# Store the real IDs and credentials only in Convex/Vercel secrets.
